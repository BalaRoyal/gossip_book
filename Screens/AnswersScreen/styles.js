import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  screen: {
    display: "flex",
    flex: 1,
    padding: 5,
    paddingTop: 30,
    backgroundColor: "#fff",
  },
});
