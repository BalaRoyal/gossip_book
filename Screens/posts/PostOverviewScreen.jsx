import React from "react";
import { StyleSheet } from "react-native";
import { FlatList, Text, Input } from "react-native-gesture-handler";

const PostOverviewScreen = (props) => {
  const listHeaderContent = <View></View>;
  return <FlatList></FlatList>;
};

export default PostOverviewScreen;
